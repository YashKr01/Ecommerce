package com.shopping.bloom.restService.callback;

import com.shopping.bloom.model.wishlist.recommendations.RecommendationItem;

public interface RecommendationProductClickListener {
    void recommendationProductClickListener(RecommendationItem item);
}
